[phases.build]
cmds = ["...", "mkdir -p /app/images", "chmod -R 777 /app/images"]

[start]
cmd = "mkdir -p /app/images && chmod -R 777 /app/images && node /assets/scripts/prestart.mjs /assets/nginx.template.conf /nginx.conf && (php-fpm -y /assets/php-fpm.conf & nginx -c /nginx.conf)"